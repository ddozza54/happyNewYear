const monthlyNav = document.querySelector("#monthlyNav");
const monthlyPage = document.querySelector(".monthlyTodo");

const onScroll = () => {
  console.log(hello);
};

monthlyNav.addEventListener("click", onscroll);
